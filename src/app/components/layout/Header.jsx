import React from 'react';

const svg = require('images/test.svg');

const Header = () => (
  <header className="main-header">
    header
    <img alt="logo-react" src={svg} />
  </header>
);


export default Header;
